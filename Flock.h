#ifndef BOIDS_H
#define BOIDS_H
#include "vector3.h"
#include "object.h"
#include "Boid.h"

#include <vector>
#include <iostream>
/*
using std::vector ;

/* A flock is a vector of boids that will flock together
class Flock {
    vector<Boid> boids;
};

void update_flock(vector<Boid> &bs, vector<InvertedBox>& iboxes, int x_max, int y_max, int z_max) ;
void update_boid(Boid* b, vector<Boid> &bs, vector<InvertedBox>& iboxes, int x_max, int y_max, int z_max) ;
void seperate(float s, const PhysicsObject &b, const vector<Boid> &bs, V3 *sep) ;
void cohesion(float x, const PhysicsObject &b, const vector<Boid> &bs, V3 *coh) ;
void align(float x, const vector<Boid> &bs, V3 *ali) ;
V3 centroid(const vector<Boid> &bs) ;
void debug_boid(Boid* b) ;
void orbit(vector<Boid> &bs, V3 v, float angle) ;
void gravity(float mass, V3* m, const Boid* b, V3* v) ;
vector<Boid> generate_boids(int max_x, int max_y, int max_z, int number) ;
*/

#endif